import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.daveWasTakn.nonogram',
  appName: 'Nonogram',
  webDir: 'dist/nonogram/browser'
};

export default config;
